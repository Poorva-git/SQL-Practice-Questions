SELECT emp_name, UPPER(emp_name) AS upper_name
FROM employee;

SELECT emp_name, LOWER(emp_name) AS lower_name
FROM employee;

SELECT dept_name, UPPER(dept_name)
FROM department;

SELECT city, LOWER(city)
FROM employee;
